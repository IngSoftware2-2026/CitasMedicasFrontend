import { Injectable } from '@angular/core';

/**
 * @deprecated Este servicio no se utiliza. El dashboard consume los servicios reales directamente.
 */
@Injectable({ providedIn: 'root' })
export class DashboardStats {
}
